services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bizaiafrica
      POSTGRES_USER: bizai
      POSTGRES_PASSWORD: change-me
    ports: ["5432:5432"]
    volumes: ["postgres_data:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bizai -d bizaiafrica"]
      interval: 5s
      timeout: 3s
      retries: 10
volumes:
  postgres_data:
