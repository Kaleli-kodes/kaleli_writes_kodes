# Privacy

Financial records are sensitive. The browser MVP stores data only in the current browser; clearing browser data removes it. The production service must provide export/delete, consent controls, purpose limitation, tenant isolation, encrypted transit/storage, minimal audit metadata and no model training on business records without explicit lawful permission.
