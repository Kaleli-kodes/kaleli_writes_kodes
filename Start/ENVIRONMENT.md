# Environment variables

`DATABASE_URL`, `AUTH_SECRET`, `APP_URL`, `S3_ENDPOINT`, `S3_BUCKET`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `AI_PROVIDER`, `AI_API_KEY`, `ERROR_MONITOR_DSN`, `REDIS_URL`. Set separate values for development, staging, and production. Never commit them.
